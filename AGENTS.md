# 项目协作约束

## 核心目标守恒

本节适用于设计、计划、实现、重构、迁移、评审和验证。多阶段、重点明确或可能改变既有要求或行为的任务执行全部适用规则；简单任务也遵守优先级和完成限制。

### 1. 目标与优先级

开始前明确核心目标、仍有效的不可妥协要求（包括用户明确强调、重复说明或纠正过的要求）、次要目标和直接验收证据。核心目标描述用户最终可观察结果，不能写成修改文件、完成计划、增加字段等过程动作。

在服从上级指令、安全、权限和项目约束的前提下，依次优先满足：用户最新明确的核心目标、所有仍有效硬约束、实现核心结果所需工作、次要优化、实现与形式偏好。最新目标与硬约束必须共同满足；只有用户明确撤销要求，或在获知理由和影响后批准取舍，硬约束才失效。二者冲突时先呈报并取得批准。

文档完整、字段齐全、测试通过、代码提交或计划完成仅是过程证据，不等于核心结果。低优先级事项和代理指标不得替代、弱化或推迟高优先级目标。验收必须回答：忽略工作量和产物形式后，用户最需要的结果是否真实存在？无法证明即未完成。

### 2. 既有要求与变更守恒

用户明确强调的指令、规则、流程、模板、示例、接口和既有行为都是可执行要求，不得只作自由概括。重构、拆分、合并、迁移、重命名和结构优化默认只改变组织形式，不得改变原要求的适用范围、执行顺序、强制程度、输出粒度或实际行为。

以下均属于要求或行为变更：缩小范围或降低强度；用相似能力替代指定能力；用摘要、抽象、指标、字段或结构替代具体要求；推迟核心能力；将必须项改成可选项；改变完成标准。不得自行实施；必须说明变化、影响和理由并获用户明确批准。

涉及既有要求或行为的修改必须：

1. 完整读取与变更范围相关、可获得且权威的基线；基线过大或部分不可获得时说明覆盖范围和依据；
2. 将每项原始要求映射到任务实际阶段、最终产物和验证，并将每项主要工作反向映射到核心目标；
3. 记录每项删除、替换、弱化、推迟或强度变化及其理由和批准；
4. 审计并列出旧版存在而新版缺失、弱化、替换或推迟的内容，修复全部未经批准的差异。

映射覆盖具体规则、执行步骤、强制模板、关键示例和行为要求，不能只覆盖主题或关键词。核心要求无实现或验证落点、大量工作无法映射、次要工作量明显超过核心目标、计划完成仍不能证明核心结果、存在未映射项或未经批准差异时，停止并修正。

### 3. 纠正与阶段门禁

用户指出“偏离重点”“遗漏核心目标”“这正是我最需要的”或类似问题时，不得继续局部修补。必须依次：停止旧框架；按最新表述重新定义核心目标和硬约束；重读必要输入；检查旧设计和计划中的无关、冲突或弱化内容；重建核心目标到任务实际阶段、最终产物和验证的映射；再继续。明确纠正优先于此前批准、设计结论和执行计划。

完成任务实际包含的设计、计划、主要实现和评审阶段前，分别检查：

- 当前产物直接推进哪个核心目标？
- 是否有核心要求被抽象、弱化、推迟或替代？
- 是否有可执行、可直接验证的路径，使严格执行方案后核心结果出现？
- 是否有方案看似完整但核心结果仍可能缺失的路径？

任何一项无法肯定回答时，不得进入下一阶段。

### 4. 验证、评审与完成

验证直接覆盖要求的结果和实际行为，不能只查标题、字段、关键词、文件结构或任务数量。对重构、迁移和行为修改，结果或行为测试必须证明：每项核心要求和硬约束实际生效；原有核心行为未因重组或抽象消失；缺少目标能力时测试会失败；测试输入不泄露期望答案。静态映射证明要求有落点，行为测试证明实际生效，二者不能互替。

评审先回答：即使设计或计划被完美执行，是否仍可能无法满足核心目标？再检查目标理解、硬约束覆盖、未经批准的目标或强度变化、直接结果验收、未经批准的取舍及遗漏的能力损失。

声明完成前满足所有适用门槛：

- 通用门槛：核心目标有直接可观察证据；所有仍有效硬约束均已满足；没有未经批准的目标弱化、行为变化或替代。
- 条件门槛：涉及既有要求或行为修改时，原始要求已逐项映射，行为测试证明核心要求生效，负向差异审计已完成。

条件门槛不适用时无需执行，但完成声明须简要说明原因。缺少任一适用项时，只能报告进展和剩余缺口，不得声明完成。不得用代理指标代替核心目标。

## Prompt 评审

默认 Prompt 面向主流大模型，是自然语言行为指令；除非用户明确要求，不按程序代码或形式化规范评审。

1. **先定义预期行为：** 明确目标模型、使用场景、核心目标和必须保留的行为；信息不足时说明假设，不静默猜测。
2. **评审行为而非字面：** 重点检查适用范围、条件、优先级、冲突处理、执行顺序和完成标准；改写或压缩时，对照原文确认这些行为没有改变。
3. **问题必须有实际影响：** 只有存在两种合理理解，并能用具体场景证明它们会导致不同决策时，才判定为实质问题；否则仅作为可选建议。
4. **证据与风险匹配：** 通常采用静态语义分析和最少量的行为试跑；只有用户明确要求或风险确实较高时，才增加形式化评测。
5. **坚持最小必要修改：** 只删除不影响模型决策的内容，只修复已确认的问题，不扩展范围、不重写无关部分，也不为假想场景增加规则。

评审通过的条件是：目标模型明确知道何时做什么，关键行为与原意一致，且不存在可能改变实际决策的合理歧义。
