# 项目协作约束

本仓库用于开发和维护 Agent Skills。以下规则适用于所有在本仓库工作的 AI Agent。

## 1. 目标与授权

开始任务前明确用户最终可观察的结果、硬约束和直接验收证据。用户明确的指令、规则、流程、模板、接口及既有行为均为要求；调整组织形式不得改变范围、顺序、强度、粒度、行为或完成标准。存在会导致不同行为的合理解释，或需要缩小、替代、推迟要求时，先说明影响并取得批准。

用户纠正方向后，最新要求优先于此前批准、设计和计划；停止冲突的旧方案，重新核对目标、实现与验证，并清理冲突或弱化内容。

在任务开始、阶段切换或关键事实变化时，先从验收角度核对下一步所依赖的要求、授权与检查结论。结合当前事实判断其是否成立，沿用仍有效的授权和证据；对缺口按本文的对应规则处理，前置条件满足后继续。

### 审批与执行

- 相关需求和设计获用户明确批准后，才能编写执行计划。
- 计划获明确批准，且用户针对该计划明确发出“开始执行”“按计划实施”等指令后，才能创建或修改实现代码。批准与执行授权可以在同一消息中给出，但须分别明确。
- 单独的“确认”“批准”等审批性回复只批准内容，不授权执行。未回复、仅查看、批准其他阶段或此前的一般性同意，均不构成当前阶段的批准或执行授权。不得因任务简单、紧迫或方案明确而跳过门禁。

### 通知授权

“完成后通知我”等请求只授权状态通知，不批准计划、不授权执行，也不扩大或追认原有授权。按收到请求时的任务状态处理：

- 有正在执行的任务：该任务执行完后发送一次结果通知。
- 没有正在执行的任务：发送一次前一条任务的当前状态，如实说明未开始、待批准、暂停或受阻；不得为了通知而启动、恢复或继续任务。

接收人使用用户指定对象，未指定则沿用已确认配置，不自行推断或扩大范围。通知与执行即使写在同一消息中，也须独立判断；不能从通知措辞或发送时机反推执行授权。

## 2. Skills 与工作流

开始任务前检查可用 Skills；用户点名或任务匹配其适用范围时，必须先完整读取并遵循。必需 Skill 不可用或无法完整读取时，停止对应工作并报告，不凭记忆代替。外部 Skill、插件或工具的默认路径和流程与本文冲突时，以本文为准。

| 触发条件 | 必用 Skill |
| --- | --- |
| 创建或修改 Skill | `skill-creator` |
| 编写或修改 `SKILL.md`、`AGENTS.md`、`CLAUDE.md` 等 Agent 指令 | `writing-for-agents` |
| 编写或修订设计；代码评审的最后检查 | `karpathy-guidelines`，分别执行下文设计门禁与代码评审规则 |
| 声明任务完成前 | `verification-before-completion`，完整执行证据验证 |

**superpowers 例外规则：** 除 `verification-before-completion` 必须自动调用外，其他 `superpowers` Skill 仅在用户于当前对话针对当前任务明确要求时调用或加载。任务匹配、此前同意或其他工具授权均不算授权。若误加载，停止应用其引入的流程，按原任务与项目约束继续，无需暂停任务；不得删除 Skill 文件或声称已从上下文移除内容。完成验证的例外不授权其他 `superpowers` 流程。

按需读取以下工作流，正文规则在对应文件维护：

- 读取、发布或更新规格与 tickets：读取 [issue-tracker](docs/agents/issue-tracker.md)，使用所属范围的 `specs/` 原生 Markdown 工作流。
- 涉及分诊或 Agent 执行状态：读取 [triage-labels](docs/agents/triage-labels.md)，使用默认五类标签，与文档人工评审状态分开记录。

## 3. 工作区与 Git 边界

### Git 安全

- **主分支只读：** 禁止改变本地主分支引用；主分支检出时禁止写工作树或索引，即使用户要求也不能通过再次确认解除。需要写入时，先在不改动主分支内容与历史的前提下，于当前 checkout 切换到非主分支。
- **worktree 默认禁用：** 创建、注册或迁移到额外 worktree 前，须取得用户在当前任务中的明确批准；项目规则、工具默认、Skill 流程、隔离需要和此前授权不能代替批准。
- **强制推送逐次确认：** 每次操作前重新取得明确确认，说明“强制推送”、目标远程/分支及改写历史的风险；不得用模糊措辞或其他操作授权代替。例如：`⚠️ 本次操作将强制推送到远程 <remote>/<branch>，可能改写远程提交历史。是否确认执行本次强制推送？`

### 产物位置

与主要逻辑无直接关系的内容（包括评估结构/结果、测试结果、执行日志、运行报告和临时验证产物）必须写入 Git 工作区外的临时目录，无论是否被 Git 跟踪。确需写入工作区时，先取得用户对具体内容与路径的显式批准。

## 4. 需求、设计与执行计划

### 文档位置与格式

用户审阅的需求、设计和执行计划正文使用中文；代码、命令、路径、接口、专有名词及必要原文引用可保留原语言。使用 Markdown，沿用所属规格目录的中文标题、版本、状态、创建/修订日期、适用范围等元数据及编号章节。

规格目录按范围选择：单模块用 `<module>/specs/`，跨模块或仓库整体用根目录 `specs/`。下表路径均相对选定的规格目录：

| 文档 | 路径 |
| --- | --- |
| 需求 | `YYYY-MM-DD-<topic>-requirements.md` |
| 设计 | `YYYY-MM-DD-<topic>-design.md`，或同日期前缀的 implementation 等后缀 |
| 实施路线图 | `plans/<topic>/README.md` |
| 阶段 ticket | `plans/<topic>/YYYY-MM-DD-NN-<step-title>.md` |
| 计划索引 | `plans/README.md` |

规格不得写入 `docs/`、`docs/superpowers/` 等默认目录；任一 `specs/` 下除 `plans/` 外不新增目录层级。范围或命名不确定时先询问用户；未经明确要求不迁移或重命名既有文档。创建或修改规格后检查归属位置，确保其他目录没有副本。

### 设计门禁

本轮首次修改设计前完整读取 `karpathy-guidelines`，在工作计划或待办中单列“Karpathy 精简与自检”，不与格式、链接或通用验证合并。以下五项既是设计检查要求，也是交付说明必须报告的证据：

1. **基线：** 修改前读取权威需求、相关设计与源码，列出影响方案的既有接口、行为和限制。
2. **假设与差异：** 明确假设、不确定项和取舍；行为歧义须询问或列为待批准差异，并说明是否存在未经批准的行为变化。
3. **最小方案：** 比较满足同一要求的可行方案，优先选择机制更少、维护成本更低者，解释选择及拒绝更简单方案的原因；无更简单的可行替代时说明限制，不编造备选或以不满足要求的方案自证精简。新增能力须有需求依据。
4. **删除审计：** 逐节对应需求、决策或验证，删除/合并重复、无关及仅用于显得完整的内容，并报告删改；不得削弱或推迟要求。
5. **直接验收：** 说明哪些成功标准能证明用户可观察的核心结果，遵守下文验收规则。

任一证据缺失，设计仍为草稿，不得标为待评审、提交、请求批准或进入执行计划。读取 Skill、声明使用、格式/链接/行数检查不能代替门禁。

### 执行计划

计划与规格同属一个范围，`<topic>` 与规格主题一致。路线图说明阶段目标、顺序、依赖和整体完成标准。两个或以上有先后依赖、可独立审阅或验收的阶段须分别建 ticket；单阶段也使用编号命名，不用 `plan.md`。是否分阶段不确定时先询问用户。

遵循所属 `plans/README.md` 的目录、链接、粒度及测试门禁；创建或修改计划时同步索引。编写和执行仍须分别满足第 1 节审批要求。

## 5. 实现与验证

### 控制复杂度

- **范围与复用：** 方案确定前优先评估复用或替换，在现有设计或工作说明中简列影响范围及复用、新增、删除关系；新增机制说明必要职责和并存理由。必要独立核验不视为重复，不另建轮廓文档。
- **及时重审：** 开始修复或代码评审前，核对本次方案及相关修复历史。修复增加机制、改变持久记录契约或扩大职责，或同一机制连续两轮修复仍出问题时，须先重审：在现有设计、工作说明或评审报告中，依据正常/失败场景说明根因，比较继续修补与收敛或替换方案，并给出选择理由。上述证据齐备前可继续定位缺陷，但不得实施相关机制的后续修补、将其局部补丁清单交付为可执行方案，或推进依赖该重审结论的工作。行为变化沿用审批门禁；授权内等价清理无需重复许可。
- **同步清理：** 清理本次变更造成的废弃实现，交付时说明删除、保留及仍重复的部分。测试随行为维护，不为旧测试保留旧机制；重组仍须覆盖原有行为与独立缺陷。

### 验证与完成

- 按风险选择足以证明用户结果的最低成本验证；纯规则不重复运行完整流程，依赖真实环境的行为仍须实测。
- 实现变更运行项目已有且适用的静态检查，覆盖变更及受影响范围；无适用工具时做针对性检查并说明局限，不为此新增工具链。
- 核查差异范围与验证成本；代码、测试规模或耗时明显增长时说明原因，检查重复机制和验证。规模仅作异常信号，不设删行配额或以数量证明质量。
- 提交前运行 `git diff --check`，检查规格与计划的相对链接有效。

声明完成前按必用 Skill 验证：核心结果已出现、硬约束满足，且无未经批准的缺失、弱化或行为差异。测试通过、文件存在、结构匹配、格式检查或提交完成均不能替代直接验收；不满足时只报告进展与缺口。

## 6. 代码评审

评审开始时先检查第 5 节的重审条件与证据。

每次代码评审汇总发现后、发布结论前，必须完整读取并使用 `karpathy-guidelines`，单独终审实现与全部评审建议，检查过度实现和过度评审：

1. 对照需求与源码，识别超范围和不必要机制，不仅凭规模或代码气味判断。
2. 阻断项须有既有要求或正确性证据，并说明场景、影响和最小修正；新增要求单列待确认，偏好及无实际影响的假想问题不阻断。
3. 检查建议能否通过删除、复用或替换解决，避免继续叠加；保留硬约束和必要独立核验，不掩盖确定缺陷，行为变化列明差异并待批准。

最终报告用具体依据简述终审发现、建议的撤回或改写、必要问题及待确认范围，并引用适用的重审证据；无问题也须明示。常规检查、测试通过、声明使用 Skill 或“无需新增机制”等结论性声明不能代替上述证据。

适用的检查或证据缺失时，标记“评审未完成”，只报告已确认发现与缺口；不得宣告通过、建议或开展依赖该评审结论的实施，也不得将缺失检查推迟到相关实施之后。此后建议有变，重新检查受影响部分再发布。

## 7. Prompt 评审

默认 Prompt 面向主流大模型，按自然语言行为指令评审；除非用户明确要求，不按程序代码或形式化规范处理。

1. 明确目标模型、场景、核心目标及必须保留的行为；信息不足时说明假设，不静默猜测。
2. 检查适用范围、条件、优先级、冲突处理、顺序与完成标准；改写或压缩须对照原文保持行为。
3. 仅当存在两种合理理解，且具体场景能证明会导致不同决策时，认定为实质问题；否则列可选建议。
4. 通常采用静态语义分析和最少行为试跑；用户明确要求或风险确实较高时才增加形式化评测。
5. 只删除不影响决策的内容、修复已确认问题，不扩展范围、重写无关部分或为假想场景加规则。

通过条件：目标模型知道何时做什么，关键行为与原意一致，无可能改变实际决策的合理歧义。

## 8. 产品版本与变更日志

- 同一未发布版本的实现、复审和修复合并维护一个 CHANGELOG 条目；按最终行为去重，不按修复轮次或测试次数新增版本。
- 只有用户明确要求发布或指定升级产品版本，才新增产品版本号或执行发布；更新 CHANGELOG、测试通过、任务完成、Git 提交均不构成发版授权。未确定版本号的变更放入“未发布”。
- 已发布记录保留；未发布条目可持续重写，只保留最终兼容性变化、限制与验收结论，不累积修补过程。规格文档修订版本与产品版本分开管理。
